version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_MULTIPLE_DATABASES: user_db,product_db,order_db,payment_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sh:/docker-entrypoint-initdb.d/init-db.sh
      # To support multiple databases, we can use a custom script or just manual creation.
      # For simplicity in this demo, I'll use a single DB and separate schemas if needed, 
      # but the user asked for "for each service". 
      # A better way is to use a script in docker-entrypoint-initdb.d/
    networks:
      - ecommerce-network

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - ecommerce-network

  zookeeper:
    image: confluentinc/cp-zookeeper:latest
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    networks:
      - ecommerce-network

  kafka:
    image: confluentinc/cp-kafka:7.4.0
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:29092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    networks:
      - ecommerce-network

  user-service:
    build: ./user-service
    restart: always
    env_file:
      - ./user-service/.env
    depends_on:
      - postgres
      - kafka
      - redis
    networks:
      - ecommerce-network
    volumes:
      - ./user-service/src:/app/src

  product-service:
    build: ./product-service
    restart: always
    env_file:
      - ./product-service/.env
    depends_on:
      - postgres
      - kafka
      - redis
    networks:
      - ecommerce-network
    volumes:
      - ./product-service/src:/app/src

  order-service:
    build: ./order-service
    restart: always
    env_file:
      - ./order-service/.env
    depends_on:
      - postgres
      - kafka
      - redis
    networks:
      - ecommerce-network
    volumes:
      - ./order-service/src:/app/src

  payment-service:
    build: ./payment-service
    restart: always
    env_file:
      - ./payment-service/.env
    depends_on:
      - postgres
      - kafka
      - redis
    networks:
      - ecommerce-network
    volumes:
      - ./payment-service/src:/app/src
  api-gateway:
    build: ./api-gateway
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - ./api-gateway/.env
    depends_on:
      - user-service
      - product-service
      - order-service
      - payment-service
    networks:
      - ecommerce-network
    volumes:
      - ./api-gateway/src:/app/src

networks:
  ecommerce-network:
    driver: bridge

volumes:
  postgres_data:
